# {Issue ID} - 发布文档

<!--
================================================================================
ISSUEKIT-RELEASE · 执行者须知（AI / 填写人必读）
--------------------------------------------------------------------------------
从本模板生成「交付用」issues/{issue-id}/release-note.md 时：正文从「状态」标题起至文末，
且必须删除本 HTML 注释全文（含本段）。用户不应在发布文档中看到 MCP 查询过程。

【何时必须主动查 MCP】
在填写 §3.3、§3.4 的「是否确认」前，若工作区已启用且可用的 MCP 包含：
  · MySQL（如 user-mysql）只读：表结构 / DDL 与迁移脚本对照
  · Apollo（如 user-apollo-*）只读：namespace / key / 与文档列出的配置项对照
且已按项目规范确认 MCP 连接归属与当前 Issue / 项目 / 目标环境一致 → 必须先执行只读核对，
再据实填写「是 / 否 / 待确认」。禁止未核对就填「是」或臆造结果。如果是自动确认，在结果后面加上“（AI）”

【建议调用的只读能力（名称以工作区 MCP 为准）】
  · 数据库：对 §3.3 涉及表使用 get_create_table / describe_table 等与迁移 SQL 比对
  · 配置：对 §3.4 列出各 key 使用 apollo_get_key（带 appId、cluster、namespace）

【交付物中禁止出现】
  · MCP 原始 JSON、完整查询步骤、逐条命令日志
  · 密钥、Token、密码、完整连接串（「值」列可写「已配置，见 Apollo」或脱敏摘要）

【交付物中仅保留】
  · §3.3 / §3.4 表格中的「是否确认」结论，及与公开文档相称的说明/非敏感值
================================================================================
-->

> **状态**: 待发布 / 已发布 / 已回滚
> **作者**: {姓名}
> **发布日期**: {YYYY-MM-DD}
> **代码分支**: {branch-name}
> **关联需求**: [需求文档](./requirement.md)
> **关联方案**: [技术方案](./technical-design.md)
> **关联测试**: [测试方案](./test-plan.md)

---

## 1. 变更概述

{一段话描述本次发布的核心变更}

## 2. 需求变更汇总

> **填写说明**：汇总中途发生的需求或方案调整时，合并以下三处信息并去重，再填入下表。
>
> 1. [需求文档](./requirement.md) 末尾「**变更记录**」表  
> 2. [技术方案](./technical-design.md) 末尾「**变更记录**」表  
> 3. 本 Issue 关联代码分支的 **Git 提交记录**（如 commit message、合并进主线的相关提交）中体现的需求或方案变更  
>
> 上述文档的变更记录里，**仅标注「初稿」且无实质变更描述的条目可忽略**。若整个周期内无中途调整，本节可写「无」或保留一行「无」说明。

| 日期 | 类型 | 变更内容 | 影响 |
|------|------|---------|------|
| {日期} | 需求变更/方案调整 | {内容} | {影响} |

## 3. 变更清单

### 3.1 功能变更

| 变更项 | 类型 | 说明 |
|--------|------|------|
| {功能} | 新增/修改/删除 | {说明} |

### 3.2 接口变更

| 接口 | 变更类型 | 兼容性 | 说明 |
|------|---------|--------|------|
| POST /api/xxx | 新增 | - | {说明} |

### 3.3 数据库变更

| 操作 | 表 | SQL | 说明 | 是否确认 |
|------|-----|-----|------|---------|
| ALTER | {表} | ALTER TABLE ... | {说明} | 是 / 否 / 待确认 |

**SQL 脚本位置**: [`sql/{version}/{script}.sql`](sql/{version}/{script}.sql)

### 3.4 配置变更

| 配置项 | 位置 | 操作 | 值 | 说明 | 是否确认 |
|--------|------|------|-----|------|---------|
| {key} | Apollo/{namespace} | 新增 | {value} | {说明} | 是 / 否 / 待确认 |

## 4. 影响范围

| 功能/流程 | 影响程度 | 说明 |
|----------|---------|------|
| {功能} | 直接变更/需回归 | {说明} |

## 5. 部署步骤

1. [ ] 执行数据库迁移: `sql/xxx.sql`
2. [ ] 更新配置: {namespace} - {key}
3. [ ] 部署服务: {服务名}
4. [ ] 验证功能: {验证步骤}
5. [ ] 监控观察: 关注 {指标} 持续 {时长}

## 6. 回滚方案

1. [ ] 回滚服务: {version/commit}
2. [ ] 回滚数据库: {回滚 SQL}
3. [ ] 回滚配置: {说明}
4. [ ] 验证回滚: {步骤}

## 7. 监控与告警

| 监控项 | 正常范围 | 告警阈值 | 观察时长 |
|--------|---------|---------|---------|
| {指标} | {范围} | {阈值} | {时长} |

## 8. 发布后验证

- [ ] {验证项}
- [ ] 监控指标正常
- [ ] 无异常告警

## 变更记录

| 版本 | 日期 | 变更人 | 变更内容 | 原因 |
|------|------|--------|---------|------|
| v1.0 | {日期} | {姓名} | 初稿 | - |
